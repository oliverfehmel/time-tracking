{% extends 'base.html.twig' %}

{% block title %}{{ 'password_reset.check.title'|trans }}{% endblock %}

{% block body %}
    <div class="page-center">
        <div class="card shadow-sm auth-card">
            <div class="card-body">
                <div class="mb-3">
                    <h1 class="h3 mb-1">{{ 'password_reset.check.title'|trans }}</h1>
                    <p class="text-muted mb-0">{{ 'password_reset.check.subtitle'|trans }}</p>
                </div>

                <div class="alert alert-info mb-3">
                    <i class="fa-regular fa-clock me-1"></i>
                    {{ 'password_reset.check.valid_for'|trans }}
                    {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </div>

                <div class="d-flex justify-content-between align-items-center gap-2">
                    <a class="small text-decoration-none" href="{{ path('app_forgot_password_request') }}">
                        {{ 'password_reset.check.retry'|trans }}
                    </a>

                    <a class="btn btn-primary" href="{{ path('_login') }}">
                        <i class="fa-solid fa-right-to-bracket me-1"></i>
                        {{ 'general.back'|trans }} zum {{ 'auth.login'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
