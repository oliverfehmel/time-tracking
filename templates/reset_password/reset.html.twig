{% extends 'base.html.twig' %}

{% block title %}{{ 'password_reset.reset.title'|trans }}{% endblock %}

{% block body %}
    <div class="page-center">
        <div class="card shadow-sm auth-card">
            <div class="card-body">
                <div class="mb-3">
                    <h1 class="h3 mb-1">{{ 'password_reset.reset.heading'|trans }}</h1>
                    <p class="text-muted mb-0">
                        {{ 'password_reset.reset.subtitle'|trans }}
                    </p>
                </div>

                {{ form_start(resetForm, { attr: { class: 'd-grid gap-3' } }) }}

                {{ form_row(resetForm.plainPassword, {
                    attr: {
                        placeholder: 'user.first_password'|trans,
                        autocomplete: 'new-password'
                    }
                }) }}

                <div class="d-flex justify-content-between align-items-center gap-2 mt-1">
                    <a class="small text-decoration-none" href="{{ path('_login') }}">
                        {{ 'general.cancel'|trans }}
                    </a>

                    <button class="btn btn-primary" type="submit">
                        <i class="fa-regular fa-floppy-disk me-1"></i>
                        {{ 'password_reset.reset.save'|trans }}
                    </button>
                </div>

                {{ form_end(resetForm) }}
            </div>
        </div>
    </div>
{% endblock %}
