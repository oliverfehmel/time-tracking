{% extends 'base.html.twig' %}

{% block title %}Neuer User{% endblock %}

{% block body %}
    <div class="tt-page">
        <div class="d-flex align-items-end justify-content-between flex-wrap gap-3 mb-3">
            <div>
                <h1 class="h3 mb-1">Neuer User</h1>
                <p class="text-muted mb-0">Lege einen neuen Benutzer an.</p>
            </div>

            <a class="btn btn-sm btn-outline-secondary" href="{{ path('_admin_users_index') }}">
                <i class="fa-solid fa-arrow-left me-1"></i>
                Zurück
            </a>
        </div>

        <div class="card shadow-sm">
            <div class="card-body">
                {{ form_start(form, { attr: { class: 'row g-3' } }) }}

                <div class="col-12 col-md-6">
                    {{ form_row(form.name) }}
                </div>

                <div class="col-12 col-md-6">
                    {{ form_row(form.email) }}
                </div>

                <div class="col-12 col-md-6">
                    {{ form_row(form.dailyWorkMinutes) }}
                </div>

                <div class="col-12 col-md-6">
                    {{ form_row(form.roles) }}
                </div>

                <div class="col-12">
                    {{ form_row(form.plainPassword) }}
                </div>

                <div class="col-12 d-flex justify-content-end gap-2 mt-2">
                    <a class="btn btn-outline-secondary" href="{{ path('_admin_users_index') }}">
                        Abbrechen
                    </a>
                    <button class="btn btn-primary" type="submit">
                        <i class="fa-solid fa-user-plus me-1"></i>
                        Anlegen
                    </button>
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
