{% extends 'base.html.twig' %}

{% block title %}User Verwaltung{% endblock %}

{% block body %}
    <div class="tt-page">
        <div class="d-flex align-items-end justify-content-between flex-wrap gap-3 mb-3">
            <div>
                <h1 class="h3 mb-1">User Verwaltung</h1>
                <p class="text-muted mb-0">User anlegen, Rollen verwalten, Passwörter zurücksetzen.</p>
            </div>

            <a class="btn btn-sm btn-primary" href="{{ path('_admin_users_new') }}">
                <i class="fa-solid fa-plus me-1"></i>
                Neuer User
            </a>
        </div>

        <div class="card shadow-sm">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                        <tr>
                            <th>Name</th>
                            <th>Soll / Tag</th>
                            <th>E-Mail</th>
                            <th>Rollen</th>
                            <th class="text-end">Aktionen</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for u in users %}
                            <tr>
                                <td class="fw-semibold">{{ u.name }}</td>
                                <td>{{ (u.dailyWorkMinutes / 60)|number_format(2, ',', '.') }} h</td>
                                <td>{{ u.email }}</td>
                                <td>
                                    <div class="d-flex flex-wrap gap-1">
                                        {% for r in u.roles %}
                                            <span class="badge text-bg-secondary">{{ r }}</span>
                                        {% endfor %}
                                    </div>
                                </td>
                                <td class="text-end">
                                    <div class="d-inline-flex gap-2 justify-content-end">
                                        <a class="btn btn-sm btn-outline-secondary" href="{{ path('_home', {'_switch_user': u.email}) }}">
                                            <i class="fa-solid fa-user"></i>
                                            Impersonate
                                        </a>

                                        <a class="btn btn-sm btn-outline-secondary" href="{{ path('_admin_users_edit', {id: u.id}) }}">
                                            <i class="fa-regular fa-pen-to-square me-1"></i>
                                            Bearbeiten
                                        </a>

                                        <form method="post" action="{{ path('_admin_users_delete', {id: u.id}) }}"
                                              class="m-0 d-inline" onsubmit="return confirm('User wirklich löschen?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete_user_' ~ u.id) }}">
                                            <button class="btn btn-sm btn-outline-danger" type="submit">
                                                <i class="fa-regular fa-trash-can me-1"></i>
                                                Löschen
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="5" class="p-3 text-muted">
                                    Keine User vorhanden.
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
